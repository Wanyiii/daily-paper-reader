---
title: Temporal Residual Neural Radiance Fields for Monocular Video Dynamic Human Body Reconstruction
title_zh: 时间残差神经辐射场用于单目视频动态人体重建
authors: "Tianle Du, Jie Wang, Xiaolong Xie, Wei Li, Pengxiang Su, Jie Liu"
date: 2026-09-04
pdf: "https://arxiv.org/pdf/2609.04984v1"
tags: ["query:d-vision"]
score: 9.0
evidence: 时序残差NeRF实现动态人体重建、新视角渲染和新姿态合成，属于神经渲染技术
tldr: 针对动态人体重建中MLP容量有限、训练耗时且质量受限的问题，提出时间残差神经辐射场，构造与MLP架构无关的时间残差场表示视频时序信号，同时通过减少可训练参数和加速渲染提升效率，并设计多维损失精确度量像素误差。实验结果在PSNR和SSIM上超越最新方法，与Anim-NeRF和Neural Body精度相当，时间效率提升约780倍。贡献在于高效高质量的动态人体新视角渲染与姿态合成方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.04984v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1390, \"height\": 487, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.04984v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 1066, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.04984v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1153, \"height\": 1107, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.04984v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1353, \"height\": 1147, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.04984v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 958, \"height\": 171, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.04984v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1448, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.04984v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1444, \"height\": 169, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.04984v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1451, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.04984v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 907, \"height\": 168, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.04984v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 921, \"height\": 173, \"label\": \"Table\"}]"
motivation: 现有MLP动态场景重建需大量训练资源且质量受限，缺乏高效的时间信号表示。
method: 构建与MLP无关的时间残差场表示时序，集成参数精简与加速渲染设计，并用多维损失监督。
result: PSNR和SSIM优于最新方法，与Anim-NeRF和Neural Body精度相近，时间效率提升约780倍。
conclusion: 提出时间残差神经辐射场，实现高效高质量的动态人体重建与新视角合成。
---

## 摘要
在计算机视觉与图形学领域，近年来已有多种方法通过单个多层感知器（MLP）实现了静态场景中人体高质量重建。然而，MLP存在容量限制，动态场景重建需要大量训练时间和计算资源，且重建质量受到明显约束。本文提出了一种有效处理动态场景人体三维建模中复杂时空信号的方法。所提方法利用时间残差神经辐射场实现人体的新颖视角渲染和新姿态合成。为解决视频序列中时间信号的表示问题，我们构建了一个与MLP架构无关的时间残差场。其次，为提高重建效率，我们提出了一种集成方法，减少了可训练参数并加速了渲染，从而增强了网络的特征表示能力。最后，我们设计了一个多维损失函数，以精确度量预测值与实际空间像素值之间的损失。实验结果表明，与最新代表性方法相比，我们所提出的方法提高了峰值信噪比（PSNR）和结构相似性指数（SSIM）等精度指标。在保持与Anim-NeRF和Neural Body相近精度的同时，时间效率实现了近780倍的提升。

## Abstract
In the field of computer vision and graphics, high-quality reconstruction of the human body in static scenes has been achieved in recent years by a single multilayer perceptron (MLP) in a number of approaches. However, MLPs have capacity limitations, requiring substantial training time and computational resources for dynamic scene reconstruction. And the quality of reconstruction is significantly constrained. This paper proposes a method for effectively processing complex spatiotemporal signals in dynamic scene human 3D modeling. The proposed method uses Temporal Residual Neural Radiance Fields to achieve novel view rendering and new pose synthesis of human bodies.To address the problem of representing temporal signals in video sequences, we construct a temporal residual field which is not related to the MLP architecture. Secondly, to improve reconstruction efficiency, we propose an integrated approach that reduces trainable parameters and accelerates rendering, thereby enhancing the network's feature representation capability. Finally, we design a multi-dimensional loss function to accurately measure the loss between predicted and actual spatial pixel values. The experimental results show that our proposed approach improves the peak signal-to-noise ratio (PSNR) and structural similarity index (SSIM) accuracy metrics compared to the latest representative methods. It maintains similar accuracy to Anim-NeRF and Neural Body while achieving a nearly 780-fold increase in time efficiency.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机与背景）

- 论文聚焦**单目视频中动态人体三维重建**这一计算机视觉与图形学的挑战性任务。
- 现有基于 MLP 的神经辐射场（NeRF）在静态场景重建中表现出色，但受限于 MLP 容量：
  - 处理动态场景时空信号时需要大量训练时间与计算资源；
  - 重建质量受网络容量约束而难以提升；
  - 直接增加神经元数量或网络层数虽可提升表达能力，却进一步加剧推理/渲染延迟和显存开销。
- 传统三维重建方法（多视角立体匹配、网格重建）在复杂姿态、遮挡与纹理缺失情况下鲁棒性不足。
- 以往基于 NeRF 的动态人体重建如 Anim-NeRF、Neural Body 等质量较好，但训练需数小时甚至十余小时，实用性差。
- 本文目标是**高效、低资源地实现单目动态人体的高质量重建**，同时支持新视角渲染和新姿态合成。

### 2. 论文提出的方法论

#### 2.1 核心思想
提出 **TRes-NeRF（Temporal Residual Neural Radiance Fields）**：
- 将“时间残差”嵌入神经辐射场，构造一个**与 MLP 架构无关的时间残差场**；
- 通过增加时间相关可训练参数而非网络宽度，增强 MLP 对复杂时空信号的建模能力；
- 配合即时神经图形基元（Instant-NGP 的多分辨率哈希编码）和空体素跳跃，把动态人体重建训练压缩到约 1 分钟。

#### 2.2 关键技术细节
- **典型时间神经辐射场**：

  Φθ(x, t) = υn(Wn(h1 ◦ h2 ◦ … ◦ hn−1)(x, t) + bn)
  
  hi(xi, t) = υi(Wi xi + bi)
  
  网络以空间坐标 x 和时间 t 作为输入，输出颜色和密度等标量场。

- **时间残差层（Temporal Residual Layer）**：

  hi(xi, t) = υi((Wi + Wi(t))xi + bi)

  将原始线性层权重 Wi 替换为 Wi + Wi(t)，其中 Wi(t) 是与时间相关的权重残差。

- **TRes-NeRF 因子分解**：

  Wi(t) = Σ_{k=1}^{R_i} g_i(t)[k] · V_i[k]

  - g(t) 为时间特征系数，V_i 为共享的生成式参数集合；
  - 通过将时间序列划分为多个子区间并进行线性插值，减少可训练参数量、抑制过拟合风险；
  - 相比简单添加残差到层输出，该方法将“时间残差”建立在**MLP 权重**上，提升层级的时空表征能力。

- **加速渲染设计**：
  1. 输入阶段：采用刚性变换 + 空体素跳跃（occupancy grid），对空区域点跳过查询，直接令密度为零；
  2. 渲染阶段：采用 Instant-NGP 的多分辨率哈希编码存储特征网格，消除哈希冲突、降低计算量，并结合原始位置特征估计颜色与密度。

- **训练损失（多维损失函数）**：
  - L_color = MSE(R, R_oi)：颜色预测误差；
  - L_trs = MSE(α, α_oi)：透明度/可见度误差，用于减少空间漂浮伪影；
  - L_L2 = ||sigmoid(tif_op) × β||_2²：对“与时间相关的精细细节”输出施加 L2 正则化；
  - 总损失：L = ω1 L_color + ω2 L_trs + ω3 L_L2，通过加权组合提升动态细节处理能力。

### 3. 实验设计

#### 3.1 数据集与 Benchmark
- **PeopleSnapshot**：主流单目人体视频基准，包含人体绕相机旋转的动态序列，提供人体 mask 与关键点参数。
  - 包含 male/female 的 casual 与 sport 等序列。
  - 用于**新视角渲染**与新姿态合成评估。
- **NeuMan**：比 PeopleSnapshot 姿态更复杂、场景更多样，用于检验复杂姿态下的性能。
  - 序列包括 bike、citron、jogging、lab、parkinglot、seattle。
  - 使用 Openpose 估计 2D 关键点、Segment-Anything 做分割、ROMP 估计相机参数与 SMPL 参数。

#### 3.2 对比方法
- **Anim-NeRF**：基于 MLP 的 NeRF，利用 SMPL 姿态变形到规范空间重建人体。
- **Neural Body**：用统计人体模型融合时间信息，通过结构潜在编码 + MLP 映射密度与颜色。
- **InstantAvatar**：使用占用网格和空间跳跃加速，是当前高效动态人体重建代表方法。

#### 3.3 实验内容
- PeopleSnapshot 数据集上对四个普通序列（male/female 3/4 casual）做 PSNR/SSIM 指标对比。
- PeopleSnapshot 数据集上对四个更具挑战性序列（male-3-sport、male-2-sport、male-2-casual、female-4-sport）进行对比。
- NeuMan 数据集的 6 个序列上进行对比。
- **定性对比**：展示重建图像细节（面部、腿部等）以及伪影抑制情况。
- **新姿态合成实验**：在 PeopleSnapshot 上展示 TRes-NeRF 对 unseen pose 的合成效果。
- **消融实验**：
  - 时间残差网络有无的对比（质量与 GPU 显存）；
  - 是否使用 TRes 因子分解的对比（训练与渲染速度）；
  - 是否使用空体素跳跃的对比（训练时间与渲染 FPS）。

### 4. 资源与算力

- **论文中出现明确计算资源信息**：
  - Anim-NeRF：约 13 小时，需 2× RTX 3090；
  - Neural Body：约 14 小时，需 4× RTX 2080；
  - InstantAvatar：约 1 分钟，单张 RTX 3090；
  - TRes-NeRF（本文）：约 1 分钟（表 2 显示 1 minute），单张 RTX 3090；
  - 表 1 中更具体为：有 empty voxel skipping 时平均训练时间 1m38s（50 epochs），渲染 13 FPS。
- **显存开销**：消融实验中，加入时间残差后 GPU 显存从 3.8G 降至 2.4G（可能因分解降低了参数冗余）。
- 缺乏使用具体“精确-总训练时间”的统一报告，但表 2/表 4 均标注“1 minute”级别。

### 5. 实验数量与充分性

- **实验组数**：
  - 4 个 PeopleSnapshot 普通序列；
  - 4 个 PeopleSnapshot 高难度序列；
  - 6 个 NeuMan 场景；
  - 1 组新视角/新姿态定性展示；
  - 3 组消融（空体素跳跃、时间残差、TRes 因子分解）。

- **充分性评价**：
  - 优点：数据集覆盖面较广（简单旋转姿态 + 复杂日常行动），并与 3 种代表方法（慢速高质量型、快速高效型）比较；
  - 不足：
    - 没有与更多近期 NeRF 加速方法或动态人体重建方法（如 HumanNeRF、A-NeRF、PIR 等）进行广泛对比；
    - 缺少对“未见视角”更多数量的定量评估；
    - 新姿态合成主要给出定性结果，缺少对新姿态下姿态误差或用户研究；
    - 消融实验只展示均值，缺少方差/多次实验的稳定性分析。

### 6. 论文的主要结论与发现

- 在 PeopleSnapshot 与 NeuMan 数据集上，TRes-NeRF 的 PSNR 和 SSIM 在多数场景中优于同量级速度的 InstantAvatar，且可与训练慢 700+ 倍的 Anim-NeRF、Neural Body 精度相当。
- 相比 Anim-NeRF 和 Neural Body，时间效率提升接近 **780 倍**（从十几小时降到约 1 分钟）。
- 时间残差层的加入不仅提高了重建质量，还由于因子分解降低了 GPU 显存占用；
- 多分辨率哈希编码 + 空体素跳跃显著加速训练/渲染，并保持细节质量；
- 该方法特别擅长细节区域（如面部、腿部）重建，伪影更少；
- TRes-NeRF 能生成合理的新姿态，具有良好的泛化性。

### 7. 优点

- 方法设计较有新意：在 **MLP 层权重层面**加入时间残差，而非简单残差连接，是一种更细粒度的时空建模思路。
- **与 MLP 架构解耦**：时间残差块可适配多种 NeRF 主干，具有较好的通用性。
- 时间残差采用生成式共享集合（factor decomposition），有效限制参数量增长，降低显存占用。
- 综合设计“刚性变换 + 空体素跳过 + 哈希编码 + 时间残差 + 多维损失”后，在**速度与质量之间取得良好平衡**。
- 实验对比直接且高效：在单张 RTX 3090 上仅需约 1 分钟训练，快速迭代具有工程应用潜力。
- 多维损失函数兼顾颜色、透明度与细节正则化，对抑制动态场景伪影有明显帮助。

### 8. 不足与局限

- 论文自述局限：方法更适用于“成本与网络容量”为主要瓶颈的场景，**无法解决缺乏无监督约束的问题**；当遇到复杂背景、遮挡严重或自监督需求较高的挑战时，优势不明显。
- 实验验证仍偏窄：主要集中在单目人体旋转和日常走动序列，缺少对：
  - 多人交互场景；
  - 宽松衣物/大变形；
  - 极端遮挡与光照变化；
  的测试。
- 对“新姿态合成”缺少定量评测（如 3D 关节误差、图像相似度等），说服力有限。
- 与 Anim-NeRF / Neural Body 的时间比较存在硬件不公平（不同 GPU 型号和数量），但其展示的 780 倍提升仍然具参考性。
- 消融实验只给出了平均指标，没有提供多轮随机实验的标准差，难以判断差异的统计显著性。
- 论文中存在个别术语（如 tif）缺少清晰定义，公式标记与图表指向也稍显草率，读者复现时可能遇到障碍。

（完）
