---
title: A Calibration Audit of Confidence in Feed-Forward 3D Reconstruction
title_zh: 前馈三维重建中置信度的校准审计
authors: "Nanxing Nick Deng, Qing Cheng, Niclas Zeller, Daniel Cremers"
date: 2026-08-30
pdf: "https://arxiv.org/pdf/2608.29705v1"
tags: ["query:d-vision"]
score: 8.0
evidence: 对前馈三维重建模型输出的逐像素置信度进行跨模型与跨数据集的校准审计，属三维重建可靠性研究
tldr: 前馈三维重建的逐像素置信度只是损失权重，并非不确定性度量，能否预测误差未知。本文审计7个骨干、13个数据集，从排序、量级、区间覆盖等检查其可靠性。发现置信度对误差排序良好，但非训练条件下整体偏乐观：中位偏差达2.4倍，且越自信越不准，甚至损失收敛后依然如此。用每骨干每数据集两个常数的幂律可校正总体幅度，却不改变排序；场景级偏差因是形状而非平移，任何缩放无法修复。发布审计协议、结果与拟合常数；在留出数据集上拟合，中位偏差降至1.35x，使用少量标注场景可达1.12x。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.29705v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1553, \"height\": 1214, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29705v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1195, \"height\": 459, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.29705v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 856, \"height\": 481, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.29705v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 805, \"height\": 482, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.29705v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1633, \"height\": 664, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.29705v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1563, \"height\": 624, \"label\": \"Table\"}]"
motivation: 前馈3D重建的置信度由损失权重训练，并非不确定性幅度，能否作为误差预测缺少系统验证。
method: 审计7个骨干、13个数据集，从排序、校准、区间覆盖等属性评估置信度，并用幂律常数校正幅度。
result: 排序好但整体低估不确定性，中位偏差2.4倍，越自信偏差越大；损失最优也不能避免，幂律只能修幅度无法修形状。
conclusion: 发布审计协议与拟合常数；用目标数据少量标注校正可把偏差从2.4x降到1.12x，但场景级形状缺失仍不可校正。
---

## 摘要
前馈三维重建模型会输出逐像素的置信度，下游系统将其作为可靠性信号读取。该置信度是作为损失权重而非不确定性幅度训练的，能否用作误差预测尚未被度量过。我们对七个已发布的骨干模型在十三个数据集上进行了审计，并从四个属性对置信度进行评分：其对误差的排序能力如何、平均水平是否恰当、跨置信度范围是否保持一致，以及其区间是否覆盖真实值。结果表明，置信度对误差的排序效果良好，但在与训练条件不完全一致的条件下读取时，预测的不确定性过低。在所有七个模型上，中位情况偏差2.4倍，且模型越自信，误差预测偏差越大。我们证明，即使损失达到最优，这一现象也可能出现。一个在其自身损失下恢复训练的已发布模型，在几百次更新后就在训练数据上达到该最优值，但在未见帧上仍然过度自信。每个骨干模型和数据集各含两个常数的幂律可修正预测不确定性的整体幅度，且不影响排序。任何重新缩放都无法修正的是场景层面，我们将其归因于模型跨预测缺乏尺度信息。我们尝试的所有修正平均而言接近正确，但仍使三分之二的留出场景落在五点区间之外，因为场景缺失的是形状而非偏移。我们发布了审计协议、结果以及每个模型和数据集的拟合常数。在目标数据集被留出时拟合的常数使中位情况从偏差2.4倍降至1.35倍，而利用该数据集的少量标注场景重新拟合后可达到1.12倍。

## Abstract
Feed-forward 3D reconstruction models emit a per-pixel confidence that downstream systems read as a reliability signal. It is trained as a loss weight, not as an uncertainty magnitude, and whether it can be used as an error prediction has not been measured. We audit seven released backbones on thirteen datasets and score the confidence on four properties, how well it ranks error, whether its level is right on average, whether it holds across the confidence range, and whether its intervals cover the truth. The confidence ranks error well, but the predicted uncertainty is too low when it is read under conditions that are not exactly those of training. The median case is off by 2.4x across all seven models, and the error prediction is further off the more confident the model is. We show that this phenomenon can appear even though the loss's optimum is reached. A released model resumed under its own loss reaches that optimum on its training data within a few hundred updates and stays overconfident on unseen frames. A power law with two constants per backbone and dataset corrects the overall magnitude of the predicted uncertainty and leaves the ranking untouched. What no rescaling reaches is the scene, which we attribute to the model's missing knowledge of scale across predictions. Every correction we tried is close to right on average and still leaves two thirds of held-out scenes outside a five-point band, because what a scene is missing is a shape rather than a shift. We release the audit protocol, its results, and the fitted constants per model and dataset. Fitted with the target dataset held out, the constants bring the median case from 2.4x off to 1.35x, and a refit on a few labelled scenes of that dataset reaches 1.12x.