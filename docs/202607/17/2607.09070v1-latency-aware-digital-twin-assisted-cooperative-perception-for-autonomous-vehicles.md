---
title: Latency-Aware Digital Twin-Assisted Cooperative Perception for Autonomous Vehicles
title_zh: 面向自动驾驶的时延感知数字孪生辅助协同感知
authors: "Boniface Uwizeyimana, Manobendu Sarker, Abraham O. Fapojuwo"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09070v1"
tags: ["query:d-vision"]
score: 9.0
evidence: 延迟感知的数字孪生辅助协同感知
tldr: "自动驾驶协同感知受端到端延迟和通信资源限制。本文提出数字孪生辅助框架，将优化问题建模为在延迟和通信约束下最大化感知精度，并设计粗到细搜索（CTFS）算法高效求解。实验表明，CTFS达到96.6%感知精度（接近穷举搜索），计算复杂度降低85.78%，且数字孪生框架使通信成本降低50%。该工作有效平衡了延迟约束下的感知精度与通信效率。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09070v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1486, \"height\": 690, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09070v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 594, \"height\": 433, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09070v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 596, \"height\": 431, \"label\": \"Figure\"}]"
motivation: 自动驾驶协同感知面临端到端延迟和通信资源约束，难以同时保证高精度和低延迟。
method: 提出数字孪生辅助的协同感知框架，将优化问题建模为最大化感知精度受延迟和通信限制，并设计粗到细搜索算法高效求解。
result: "CTFS算法达到96.6%感知精度（接近穷举搜索），计算复杂度降低85.78%，且数字孪生框架使通信成本降低50%。"
conclusion: 所提框架在延迟约束下有效平衡感知精度与通信效率，为自动驾驶协同感知提供实用方案。
---

## 摘要
本文提出了一种数字孪生（DT）辅助的协同感知框架，旨在在端到端（E2E）时延约束下提高感知精度，并在通信资源受限条件下平衡感知精度与端到端时延。我们构建了一个在时延和通信限制下最大化感知精度的优化问题，并通过新提出的由粗到细搜索（CTFS）算法进行求解。仿真结果表明，所提CTFS算法在时延约束下实现了96.6%的感知精度，接近穷举搜索，同时计算复杂度降低约85.78%。此外，DT辅助框架通过估计的、时间同步的状态更新，实现了非DT通信成本降低50%。

## Abstract
This paper introduces a digital-twin (DT)-assisted cooperative perception framework designed to improve perception accuracy under end-to-end (E2E) latency constraints and to balance perception accuracy and E2E latency under communication resource constraints in autonomous vehicles. We formulate an optimization problem that maximizes perception accuracy subject to latency and communication limitations, and solve it using a newly proposed coarse-to-fine search (CTFS) algorithm. Simulation results show that the proposed CTFS algorithm achieves 96.6% perception accuracy, close to exhaustive search, under latency constraints while reducing computational complexity by approximately 85.78%. The DT-assisted framework further achieves a 50% reduction in the non-DT communication cost through estimated, time-synchronized state updates.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：自动驾驶协同感知中，如何在端到端（E2E）时延约束下最大化感知精度，同时平衡通信资源限制（带宽、发射功率）的影响。
- **背景**：
  - 单车感知受限于遮挡、传感器范围，协同感知通过V2X共享特征地图可提升精度。
  - 现有工作（如Where2comm [5]）引入通信置信度阈值来控制传输量，但未联合优化阈值和发射功率，且未考虑时延约束的在线预测能力。
  - 时变信道、车辆移动和计算负载导致时延易违反，传统瞬时决策方法无法提前预判。
  - 数字孪生（DT）可提供同步虚拟副本、状态预测和前瞻性优化，但尚未被用于协同感知的时延-精度联合优化。
- **整体含义**：首次提出DT辅助的协同感知框架，通过预测物理网络状态，联合优化通信置信度阈值和发射功率，在严格时延约束下实现近优感知精度，并大幅降低计算与通信开销。

## 2. 论文提出的方法论

### 核心思想
- 在DT层维护车辆与RSU的虚拟副本，利用历史与实时数据估计未来状态（位置、信道增益等）。
- 构建非凸优化问题，目标为最大化感知精度（mAP）并惩罚发射功率，约束包括E2E时延、阈值范围[0,1]、发射功率上限。
- 由于mAP函数无显式解析形式，提出**粗到细搜索（CTFS）算法**，避免梯度不可用问题。

### 关键技术细节
- **系统模型**：
  - 物理层：M辆车、N个RSU；每辆车提取BEV特征（基于PointPillars+Where2comm），通过子6GHz上行传输。
  - DT层：数据管理模块（采集）、基本与功能模块（维护虚拟副本、执行CTFS）、资源管理模块（下发控制决策）。
  - 时延建模：特征提取时延、上行传输时延（受阈值θ影响）、等待时延（最慢车辆）、融合时延、下行时延（固定0.01s）。
- **优化问题**：
  - 目标函数：$\text{obj}(θ,t)= \text{mAP}(θ_m,t) - ρ \sum_{m} p_m(t)/p_m^{\mathrm{Max}}$。
  - 约束：E2E时延 ≤ 最大容忍时延；$0<θ_m≤1$；$0≤p_m≤p_m^{\mathrm{Max}}$。
- **CTFS算法流程**：
  1. **粗搜索**：将$θ_m$离散为$q$个值，生成$q^M$候选向量。对每个候选，根据时延约束推导所需最小功率$p_m^*$（公式8），若不可行（超功率上限）则丢弃；否则评估mAP并计算目标值。选出$K$个最优候选。
  2. **细调**：在$K$个候选确定的精细化区间内再次离散（可增大$q$），重复可行性检查和目标评估，输出最优$(θ^*, p^*)$。
- **DT辅助优势**：物理环境估计器利用历史轨迹预测未来信道增益，使CTFS可提前决策；同步周期$\pi$减少通信开销（如$\pi=2$时降低50%）。

## 3. 实验设计

- **数据集**：采用UrbanIng-V2X数据集（来自[4]），基于LiDAR 3D点云，检测范围0-100米，IoU阈值0.3。
- **场景**：M=2辆车，N=1个RSU（单RSU边缘服务器）。每辆车计算能力2 TFLOPs/s，RSU 25 TFLOPs/s。
- **Benchmark与对比方法**：
  - 穷举搜索（Exhaustive search）：细网格$q=30$。
  - 随机搜索（Random selection）：随机选择可行$(θ^*, p^*)$。
  - 所提CTFS：粗搜索$q=8$，保留$K$个候选后细调。
- **评估指标**：感知精度（mAP，取前50个时隙的平均值与标准差），计算复杂度（FLOPs），通信成本（数据传输量）。

## 4. 资源与算力

- **明确数据**：
  - 论文未提供GPU型号、数量、训练时长等信息。
  - 仅提到特征提取与融合模型基于OpenCOOD框架中PointPillars和Where2comm，训练为离线进行。
  - 仿真中假设车辆计算能力2 TFLOPs/s，RSU 25 TFLOPs/s。
- **结论**：缺少训练阶段的算力细节，可复现性有限。

## 5. 实验数量与充分性

- **实验组数**：
  - 对比不同最大时延（$T_{\mathrm{Max}}=0.1\mathrm{s}, 0.3\mathrm{s}$）下三种方法的mAP（图2）。
  - 对比不同带宽（$B_m=10,15\mathrm{MHz}$）下CTFS的mAP（图3）。
  - 计算复杂度数值对比（CTFS vs 穷举搜索）。
  - 通信成本节省比例（$\pi=2$时50%）。
- **充分性评估**：
  - **优点**：关键变量（时延、带宽）被覆盖，且对比理论最优（穷举）和随机基线，验证了CTFS的近似最优性。
  - **不足**：
    - 仅单一场景（2车1RSU），未扩展至多车多RSU。
    - 未进行消融实验（如去掉DT预测、不同$K$值、同步周期$\pi$的影响）。
    - 缺乏与现有其他优化算法（如强化学习、博弈论）的对比。
    - 数据集仅用UrbanIng，未在其他数据集（如V2X-Sim、OPV2V）上验证泛化性。
    - 实验次数未报告，仅显示“前50个时隙”的平均值，未提供统计显著性分析。
- **客观性与公平性**：CTFS与穷举搜索使用相同模型和参数，随机搜索作为相对公平的弱基线，比较合理。但穷举搜索的网格密度$q=30$是否足够细未论证。

## 6. 论文的主要结论与发现

1. **精度与时延的权衡**：时延越严格，mAP下降（$T=0.3\mathrm{s}$时CTFS达96.6%，$T=0.1\mathrm{s}$时显著降低）。
2. **CTFS接近最优**：在$T=0.3\mathrm{s}$时CTFS与穷举搜索性能几乎一致，但计算复杂度降低85.78%（$13.5\times10^{12}$ vs $94.95\times10^{12}$ FLOPs）。
3. **带宽影响**：带宽从10MHz增至15MHz，mAP提升约15-20%，说明通信资源是瓶颈。
4. **DT通信节省**：同步周期$\pi=2$时，DT框架减少50%数据传输成本。

## 7. 优点

1. **方法创新性**：首次将数字孪生引入协同感知优化，利用预测能力实现前瞻性决策，而非单纯瞬时优化。
2. **CTFS算法有效性**：克服了非凸、无梯度问题，在保持近优精度同时大幅降低计算开销，适合实时应用。
3. **模型完整性**：详细建模了E2E时延的四个成分（特征提取、上传、等待、融合、下行），并给出解析功率公式（公式8）。
4. **清晰展示权衡**：通过不同时延和带宽下的实验，量化了精度与资源约束的关系。
5. **开源数据集与模型**：基于OpenCOOD等公开工具，便于复现。

## 8. 不足与局限

1. **实验规模有限**：仅2辆车、1个RSU，未验证多车多RSU场景下的扩展性；未考虑车辆动态加入/离开、通信干扰等实际因素。
2. **DT细节不足**：同步周期$\pi$未优化，仅举例$\pi=2$；物理环境估计器具体实现（轨迹预测方法）未描述，仅提及“估计”。
3. **缺乏公平对比**：未与现有其他优化方法（如基于强化学习的资源分配、博弈论、启发式算法）比较；随机搜索作为基线过弱。
4. **信道模型理想化**：路径损耗指数$\gamma=2.0$（自由空间），未考虑城市环境阴影衰落、多径等；小尺度衰落假设指数分布（均值1），验证真实性不足。
5. **数据集单一**：仅使用UrbanIng，未在其他协同感知数据集上测试，泛化性存疑。
6. **忽略实际感知挑战**：未处理数据关联、遮挡不一致、多模态融合、车辆间时间同步误差等真实问题。
7. **可复现性缺口**：未提供GPU型号、训练硬件配置、训练时长，且CTFS算法的$K$值、细调网格分辨率未明确。
8. **假设限制**：假设所有车辆参与融合，且DT模型可平滑迁移（基于高速有线连接），实际中可能存在切换延迟或连接不可靠。

（完）
