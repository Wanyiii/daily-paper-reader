---
title: Optimal Transport-based Semantic Alignment for LLM-based Audio-Visual Speech Recognition
title_zh: 基于最优传输的语义对齐用于基于大语言模型的音视频语音识别
authors: "Xugang Lu, Peng Shen, Yu Tsao, Hisashi Kawai"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09001v1"
tags: ["query:multimodal"]
score: 9.0
evidence: 基于最优传输的语义对齐用于音视频融合
tldr: 基于大模型的音视频语音识别通常独立预训练编码器后融合，但未解决跨模态表示差异。本文提出最优传输语义对齐框架，利用OT估计模态特征与LLM语言空间的概率耦合矩阵作为软伪标签进行对比学习，将视听特征对齐至语言空间。在LRS3-TED上，该方法在干净和多种噪声条件下均取得最佳性能。该框架显式缩小模态差距，显著提升多模态融合与解码效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09001v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1554, \"height\": 929, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09001v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 621, \"height\": 363, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09001v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 771, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 771, \"height\": 248, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 785, \"height\": 137, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 724, \"height\": 271, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09001v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 859, \"height\": 314, \"label\": \"Table\"}]"
motivation: 现有LLM-AVSR未显式对齐音频、视觉与文本表示，限制跨模态融合效果。
method: 通过最优传输估计模态特征与语言嵌入的耦合矩阵，作为软伪标签监督对比学习，对齐多模态表示至LLM空间。
result: 在LRS3-TED上，多种SNR下均达到当前最佳性能，大幅超越强基线。
conclusion: 最优传输对齐有效缩小模态差距，提升LLM-AVSR在噪声环境下的鲁棒性和准确性。
---

## 摘要
基于大语言模型（LLM）的音视频语音识别（LLM-AVSR）最近通过利用互补的音频和视觉信息，在恶劣声学环境中展现出强大的鲁棒性。现有方法通常采用独立预训练的音频和视觉编码器，其输出被投影并融合为软提示，以条件化LLM进行语音识别。然而，大多数方法在进行多模态融合时，没有明确解决音频、视觉和文本模态之间的表示差异，这可能限制了跨模态集成的有效性。本文提出了一种基于最优传输（OT）的语义对齐框架，用于LLM-AVSR。该方法在多模态融合之前，通过参照LLM的语言嵌入空间对齐音频和视觉表示，显式地弥合了模态差距。具体来说，使用OT来估计概率耦合矩阵，该矩阵刻画了模态特定特征与语言嵌入之间的结构化对应关系。进一步利用所得的OT耦合作为软伪标签来监督对比学习，鼓励提取语义一致且跨模态一致的音视频表示。通过将多模态特征锚定到LLM的语言空间，所提出的框架促进了更有效的多模态融合和解码。我们使用基于Whisper的音频编码器、基于AV-HuBERT的视觉编码器和LLaMA3.2-3B解码器实现了所提出的框架。在LRS3-TED基准上进行的实验表明，在广泛的信噪比（SNR）下，无论是干净还是噪声评估条件，该方法都比强基线模型有一致的改进，并达到了最先进的性能。

## Abstract
Large language model (LLM)-based audio-visual speech recognition (LLM-AVSR) has recently demonstrated strong robustness in adverse acoustic environments by leveraging complementary audio and visual information. Existing approaches typically employ independently pretrained acoustic and visual encoders, whose outputs are projected and fused as soft prompts to condition an LLM for speech recognition. However, most methods perform multimodal fusion without explicitly addressing the representational discrepancy between audio, visual and text modalities, potentially limiting the effectiveness of cross-modal integration. In this paper, we propose an optimal transport (OT)-based semantic alignment framework for LLM-AVSR. The proposed method explicitly bridges the modality gap by aligning the acoustic and visual representations with reference to the linguistic embedding space of the LLM before multimodal fusion. Specifically, OT is used to estimate probabilistic coupling matrices that characterize structured correspondences between modality-specific features and linguistic embeddings. The resulting OT couplings are further utilized as soft pseudo-labels to supervise contrastive learning, encouraging the extraction of semantically coherent and cross-modal consistent audio-visual representations. By anchoring multimodal features to the linguistic space of the LLM, the proposed framework facilitates more effective multimodal fusion and decoding. We implement the proposed framework using a Whisper-based acoustic encoder, an AV-HuBERT-based visual encoder, and a LLaMA3.2-3B decoder. Experiments conducted on the LRS3-TED benchmark demonstrate consistent improvements over strong baselines and achieve state-of-the-art performance under both clean and noisy evaluation conditions across a wide range of signal-to-noise ratios (SNRs).

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **核心问题**：现有基于大语言模型（LLM）的音视频语音识别（LLM-AVSR）系统通常使用独立预训练的音频编码器（如 Whisper）和视觉编码器（如 AV-HuBERT），直接将它们的输出投影并融合作为软提示输入 LLM，但**没有显式地处理音频、视觉和文本模态之间的表示差异**。这种表示鸿沟限制了跨模态融合的有效性，尤其在噪声环境下鲁棒性不足。
- **整体意义**：本文首次将**最优传输（Optimal Transport, OT）**引入 LLM-AVSR 的模态对齐，通过将多模态特征对齐到 LLM 的语言嵌入空间，实现语义层次的匹配，从而提升融合质量和解码性能。该工作为多模态语音识别提供了一种新的对齐范式。

### 2. 论文提出的方法论

- **核心思想**：在多模态融合之前，利用 OT 建立音频/视觉特征与 LLM 语言嵌入的**软对应关系**，并将这种对应作为**伪标签**监督对比学习，迫使模型学到语义一致、跨模态统一的表示。
- **关键技术细节**：
  - **特征提取**：音频用 Whisper 编码器→za，视觉用 AV-HuBERT 编码器→zv，目标文本用 LLM 的 token 嵌入→zt（经线性变换）。
  - **OT 对齐**：将 za、zv 和 zt 视为经验概率分布，计算熵正则化的 OT 耦合矩阵 Qa→t 和 Qv→t（式(2)(3)），通过 Sinkhorn 算法求解（式(7)(8)）。
  - **虚拟桶（Virtual Bucket）**：引入额外的哑变量吸收非信息帧（如背景噪声、静音），避免强制对齐，提升鲁棒性（图 2，式(9)）。
  - **对齐损失**：以 OT 耦合矩阵为软目标，计算交叉熵对比损失（式(10)(11)）。
  - **融合方式**：比较了通道拼接、加法、交叉注意力、Q-former 四种融合，最终选用**通道拼接**作为最佳方案。
  - **总体损失**：L_total = L_AR（自回归交叉熵）+ α * L_align（α 为权重，实验中 0.1-0.5 最佳）。
- **训练策略**：编码器冻结，LLM（LLaMA3.2-3B）用 LoRA 微调。

### 3. 实验设计

- **数据集**：**LRS3-TED**（大型野外演讲视频数据集）：
  - 训练：433 小时，163374 段；验证：1 小时，1200 段；测试：1 小时，1321 段。
  - 视频：25 fps，224×224，裁切至唇部区域 96×96（训练时随机裁剪 88×88 + 水平翻转）。
  - 音频：16 kHz，使用 Whisper 编码器，额外下采样使帧率匹配视觉。
  - 噪声模拟：训练时 75% 概率加 Babble 噪声（SNR 均匀采样 [-5, 0, 5, 10, 15, 20] dB），测试时计算 -5、0、5 dB 及干净条件。
- **基准（Benchmark）**：官方测试集，采用**词错误率（WER, %）**作为评价指标。
- **对比方法**：
  - Whisper-Flamingo、LLaMA-AVSR、MMS-LLaMA（均为近期 AVSR 工作）。
  - 自身基线：无对齐的通道拼接融合（Our method (no align)）。
  - 消融研究：不同融合方法（Table I/II）、有无虚拟桶（Table III）、不同对齐权重 α（Table IV）、多组超参数设置（Table V 中的 setting 1/2/3）。

### 4. 资源与算力

- **文中未明确说明使用的 GPU 型号、数量及训练时长**。仅提及：
  - 使用 LLaMA3.2-3B 作为 LLM。
  - LoRA 微调（秩=16，缩放因子=32）。
  - 优化器 Adam，初始学习率 1e-4，warm-up 步数 5k，总训练步数 30k。
  - 编码器（Whisper medium 和 AV-HuBERT large）被冻结。
  - 无其他硬件或时间信息。

### 5. 实验数量与充分性

- **实验数量**：共设计了**5组主要实验**（对应5个表格）：
  - Table I/II：干净/噪声训练下5种融合方法的对比（10组）。
  - Table III：虚拟桶的消融（2组，含/不含）。
  - Table IV：对齐权重 α 的 6 种取值。
  - Table V：与 3 个外部方法、3 个内部超参数设置的对比（共 7 行）。
  - 此外还进行了 **超参数交互效应分析**（提及 λ、τ、˜s 等，但未以表格展示）。
- **充分性与公平性**：
  - 实验条件严格一致（相同数据划分、训练策略、LoRA 配置）。
  - 消融实验设计完备：对比融合策略、对齐模块、虚拟桶、对齐权重，覆盖主要设计选择。
  - 与外部方法比较时，使用相同的 LRS3-TED 评测标准，具有可复现性。
  - 但**缺乏对其他数据集（如 LRS2、VoxCeleb2）的泛化验证**，单一数据集上的结果可能存在偏差。
  - 超参数寻优采用经验选取而非系统网格搜索，可能未达到最优。

### 6. 论文的主要结论与发现

- **性能提升显著**：在所有 SNR 条件下（-5 dB 到 clean），OT 对齐框架均优于强基线，达到当时 SOTA（例如在 -5 dB 时 WER 从 8.34% 降至 7.16%~7.96%）。
- **虚拟桶有效**：引入虚拟桶可轻微改善 WER（如 -5 dB 从 8.06% 降至 7.98%），有助于过滤非信息帧。
- **通道拼接融合最佳**：在噪声训练下，通道拼接显著优于加性、交叉注意、Q-former 和时间拼接。
- **对齐权重敏感但不苛刻**：α 在 0.1~0.5 范围内性能稳定，过大或过小会下降。
- **显式语义对齐提升 LLM 利用多模态信息的能力**，且对 LLM 架构无需修改。

### 7. 优点

- **方法创新性强**：首次将 OT 引入 LLM-AVSR 的模态语义对齐，提供了一种可解释的分布匹配视角。
- **虚拟桶机制新颖**：借鉴匹配领域的 outlier 处理思想，提升对无用帧的鲁棒性。
- **框架模块化**：OT 对齐模块可即插即用，不改变编码器和 LLM 结构，易于集成。
- **实验设计全面**：覆盖多种融合策略、消融关键组件、与多个 SOTA 方法对比，结论可靠。
- **性能提升幅度大**：在低 SNR 条件下 WER 相对降低 10%~15%，具有实际应用价值。

### 8. 不足与局限

- **超参数依赖经验**：OT 的 λ、耦合矩阵的 ˜s、对齐权重 α 等均采用手工调参，缺乏自动化优化分析；作者也承认超参数间存在交互作用，需进一步探索。
- **单一数据集验证**：仅在 LRS3-TED 上评测，未在 LRS2、VoxCeleb2 等其他 AVSR 基准上验证，泛化能力存疑。
- **计算资源未报告**：缺乏对训练/推理时间、GPU 型号和数量的描述，难以评估实际成本。
- **虚拟桶与 unbalanced OT 的关系未深入探讨**：文中提到相似性但未进行理论或实验对比。
- **局限性场景未覆盖**：未考虑说话人变化、域迁移（如会议、街景）或极低质量视频等场景。
- **未与端到端非 LLM 方法对比**：虽然与多个 LLM-AVSR 方法比较，但未与经典 Conformer+CTC/Attention 架构（如 Auto-AVSR）对比，后者可能在某些条件下仍有竞争力。

（完）
