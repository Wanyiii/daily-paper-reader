---
title: "UMSS: Towards Unsupervised Multi-modal Semantic Segmentation"
title_zh: UMSS：迈向无监督多模态语义分割
authors: "Haitian Zhang, Thai Duy Nguyen, Xiangyuan Wang, Mohan Liu, Lin Wang"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12372v1"
tags: ["query:multimodal"]
score: 9.0
evidence: 无监督多模态语义分割解决融合退化
tldr: "多模态语义分割依赖昂贵标注，无监督方法在多模态上存在融合退化。本文首次提出无监督多模态语义分割任务，并构建UniM2框架，通过跨模态对应协同学习统一语义空间，用跨模态协调器以RGB为参考抑制冲突。在NYU Depth v2和MFNet上分别提升6.4%和9.8% mIoU，展现明确优势。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12372v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1274, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2607.12372v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1262, \"height\": 829}, {\"url\": \"assets/figures/arxiv/2607.12372v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1268, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2607.12372v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1263, \"height\": 702}, {\"url\": \"assets/figures/arxiv/2607.12372v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1262, \"height\": 411}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12372v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1254, \"height\": 449}, {\"url\": \"assets/tables/arxiv/2607.12372v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1256, \"height\": 697}, {\"url\": \"assets/tables/arxiv/2607.12372v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1259, \"height\": 246}, {\"url\": \"assets/tables/arxiv/2607.12372v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1259, \"height\": 284}, {\"url\": \"assets/tables/arxiv/2607.12372v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 496, \"height\": 228}, {\"url\": \"assets/tables/arxiv/2607.12372v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 393, \"height\": 228}, {\"url\": \"assets/tables/arxiv/2607.12372v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 386, \"height\": 226}]"
motivation: 现有无监督语义分割在多模态数据上因融合退化难以利用互补信息，需无标注下的有效方法。
method: 基于DINOv3构建UniM2，通过跨模态对应协同学习统一空间，并引入跨模态协调器以RGB为参考抑制冲突。
result: "在NYU Depth v2和MFNet上mIoU分别提升6.4%和9.8%，超越现有无监督多模态框架。"
conclusion: 首次探索无监督多模态语义分割任务，UniM2有效利用互补传感器信息，无需标注。
---

## 摘要
多模态语义分割对于复杂环境下的稳健感知至关重要，但由于人工标注成本过高，其潜力仍未得到充分挖掘。虽然无监督语义分割在单一RGB模态上取得了强大结果，但其对多模态数据的简单扩展往往受到融合退化的阻碍。这是因为在没有显式监督的情况下，现有框架难以协调不同传感器捕获的异构结构模式，因此无法有效利用其互补信息。在本文中，我们首次尝试解决无监督多模态语义分割这一新问题，旨在完全无标签的环境中有效利用互补传感器信息。为此，我们提出了UniM2（统一多模态），一种基于DINOv3构建的新颖框架，将传统融合方法转化为一致的性能提升。我们的关键思想是学习由跨模态对应协同驱动的统一潜在空间，以提取内在的共享语义线索，从而绕过标签引导的自适应融合。为了缓解固有的模态间冲突，我们引入了跨模态协调器，将RGB指定为稳定参考，有效抑制不一致的关系监督，同时引导模型利用互补的结构特征。在NYU Depth v2和MFNet上的大量实验结果表明，UniM2的mIoU分别提升了6.4%和9.8%，相较于现有UMSS框架展现出明显优势。

## Abstract
Multimodal semantic segmentation (MSS) is essential for robust perception in complex environments, yet its potential remains largely untapped because of the prohibitive cost of human annotations. While unsupervised semantic segmentation (USS) has achieved strong results on a single RGB modality, its naive extension to multimodal data is often hindered by fusion degradation. This occurs because, without explicit supervision, existing frameworks struggle to reconcile the heterogeneous structural patterns captured by different sensors and therefore fail to effectively exploit their complementary information. In this paper, we make the first attempt to address the novel problem of Unsupervised Multimodal Semantic Segmentation (UMSS), aiming to effectively exploit complementary sensor information in a fully label free setting. To this end, we propose UniM2 (Unified Multimodal), a novel framework built on DINOv3 that transforms conventional fusion methods into consistent performance gains. Our key idea is to learn a unified latent space driven by Cross Modal Correspondence Synergy (CMCS) to extract intrinsic shared semantic cues, bypassing the need for label guided adaptive fusion. To mitigate inherent intermodal conflicts, we introduce a Cross Modal Harmonizer (CMH) that designates RGB as a stable reference, effectively suppressing inconsistent relational supervision while guiding the model to exploit complementary structural features. Extensive experimental results on NYU Depth v2 and MFNet show that UniM2 improves mIoU by 6.4% and 9.8%, respectively, demonstrating clear advantages over existing frameworks for UMSS.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **研究背景**：多模态语义分割（MSS）通过融合 RGB 与深度、热红外等传感器数据，显著提升复杂环境（如弱光、几何模糊）下的感知鲁棒性，但在安全关键场景（自动驾驶、机器人导航）中至关重要。
- **核心问题**：现有 MSS 方法严重依赖海量像素级人工标注，成本高昂且难以扩展到开放语义类别；而无监督语义分割（USS）虽在单模态 RGB 上取得突破（如 DINO 系列），但直接将其扩展到多模态数据时，由于缺乏标签对齐不同传感器的异构结构模式，出现严重的“融合退化”现象——即加入辅助模态反而导致性能下降。
- **任务定义**：本文首次定义**无监督多模态语义分割（UMSS）**，目标是在完全无标签条件下有效利用多模态互补信息，实现跨模态协同增益。

#### 2. 方法论：核心思想、关键技术细节
- **核心思想**：基于 DINOv3 冻结骨干，学习一个**统一的潜在空间**，通过**跨模态对应协同（CMCS）**作为内在监督信号，迫使融合后的低维嵌入同时对齐各模态特征中的语义相似结构；并引入**跨模态协调器（CMH）**，以 RGB 为稳定参考，自适应调节辅助模态的监督强度，抑制冲突、保留互补。
- **关键技术细节**：
  - **整体架构**：对每组输入（RGB + 辅助模态 X），分别经冻结 DINOv3 提取特征 \(f_{rgb}, f_X\)，再经模态特定网络（MSN）精炼，通过可学习的 Conv 融合模块 \(\Psi\) 得到融合特征 \(f_{fus}\)，最后经分割头 \(S\) 映射为低维嵌入 \(s\)。
  - **CMCS 损失**：训练时构建 Self/KNN/Random 成对组，对每对组（源、目标），计算各模态特征间的余弦相似度 \(F_{rgb}, F_X\)，以及融合嵌入间的相似度 \(S\)；损失函数迫使 \(S\) 近似 \(F_{rgb}\) 与 \(F_X\) 的结构：  
    \[
    L_{cmcs} = -\sum (F_{rgb} - b) \odot \max(0, S) + \lambda \sum (F_X - b) \odot \max(0, S_X)
    \]
    其中 \(S_X\) 是经过 CMH 处理的嵌入相似度（避免直接对齐冲突）。
  - **跨模态协调器（CMH）**：一个轻量两卷积分支，将主嵌入 \(s\) 转换为 \(s_X\)，仅让 \(s_X\) 受辅助模态监督，而主嵌入 \(s\) 主要由 RGB 监督。这样作为“缓冲层”，吸收互补信号的同时隔离几何/物理矛盾。可通过扩展多个独立 CMH 分支支持多辅助模态。

#### 3. 实验设计
- **数据集与场景**：
  - **NYU-Depth-v2**：室内 RGB-D 数据集（1449 对），13 类语义评估。
  - **MFNet**：城市 RGB-热红外数据集（1569 对），8 类语义评估（含日夜变化）。
  - **MCubeS**：四模态材料分割数据集（RGB、NIR、DoLP、AoLP），20 类评估，验证模型可扩展性。
- **基准（Benchmark）**：基于 USS 扩展的对比：K‑means 聚类（DINO 特征）、STEGO、EAGLE 及它们的多模态变体；同时补充图像级融合（SwinFusion、Mask‑DiFuser）和 RGB→X 蒸馏（CORAL、MMD、Cosine）作为额外参考。
- **对比方法**：主要对比 STEGO [20] 和 EAGLE [27] 的多模态版本（直接加入辅助模态特征融合），以及 UniM2 自身在不同配置下的消融。
- **评价指标**：平均交并比（mIoU）和像素准确率（Acc.）。

#### 4. 资源与算力
- 文中明确说明：所有模型在**单张 NVIDIA GeForce RTX 5090 GPU**上训练，每模型训练时间**少于两小时**。采用 Adam 优化器，学习率 \(5\times10^{-4}\)，批大小 32。
- 未提及 GPU 数量或分布式训练信息，但算力开销较低，具备较好的资源友好性。

#### 5. 实验数量与充分性
- **实验组数**：涵盖 3 个数据集（NYU‑Depth v2、MFNet、MCubeS），多种骨干（ViT‑S/16、ViT‑B/16），多种方法对比（STEGO、EAGLE 等），以及消融实验（组件、锚点位置、融合策略、超参数 λ 和偏置 b 等）。
- **充分性与公平性**：
  - **充分性**：消融覆盖了模块有效性、CMH 位置、融合算子类型、以及多种融合基线对比，并补充了超参数分析和可视化。
  - **公平性**：所有对比方法均使用相同的数据预处理（5‑crop）、骨干（DINOv3）和超参数搜索预算（200 次贝叶斯优化），避免了单一固定配置导致的不公平。不同数据集上超参数单独调优。
- **结论**：实验设计系统、公平，消融充分。

#### 6. 主要结论与发现
- **主要结论**：UniM2 将传统融合在多模态无监督环境中的**性能退化转化为显著增益**——在 NYU‑Depth v2 和 MFNet 上，相较于 RGB 基线（STEGO + DINOv3‑Base/16），mIoU 分别提升 **6.4%** 和 **9.8%**，且超过所有对比的 USS 多模态扩展方法。
- **发现**：直接堆叠现有融合模块（如 CBAM、StitchFusion）导致后融合退化；而 CMCS + CMH 能有效解决模态冲突；RGB 作为稳定参考优于 Depth 作为参考；可学习融合（Conv）优于静态操作（Sum、Mean）；CMH 设计可自然扩展至四模态并带来增量收益。

#### 7. 优点
- **方法创新**：首次正式定义 UMSS 任务并提出解决方案；CMCS 将跨模态结构一致性作为内在监督，CMH 以轻量缓冲层优雅解决模态冲突，无需标签。
- **实验扎实**：在 3 个代表性数据集、多种骨干、多种 baselines 上系统验证，消融实验覆盖组件、锚点、融合策略等关键维度，且保证公平对比。
- **资源高效**：单 GPU 两小时内完成训练，易于复现。
- **可扩展性**：CMH 模块化设计可自然支持任意数量辅助模态（如 MCubeS 四模态实验验证）。

#### 8. 不足与局限
- **实验覆盖偏差**：主要基于室内（NYU‑Depth）和城市道路（MFNet）场景，未验证更复杂或极端环境（如夜间、雨雾、大尺度室外场景）；类别数较少（13 类/8 类），对更细粒度分割（如 >50 类）未见实验。
- **模态依赖**：方法依赖 RGB 作为稳定语义参考，若辅助模态本身严重退化或噪声过大（如 AoLP 在 MCubeS 上略降），增益有限；未讨论 RGB 缺失或损坏情况。
- **潜在风险**：无监督聚类假设语义类别先验已知（K 固定），若真实类别数未知或场景开放，聚类中心与任务类别不匹配；未评估类别数不匹配时的鲁棒性。
- **应用限制**：当前框架依赖 DINOv3 预训练的固定特征，对未见语义概化能力有限；未探索与视觉语言模型（如 CLIP）结合的可能性。

（完）
