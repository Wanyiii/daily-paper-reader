---
title: "CalibBEV: LiDAR-Camera Calibration via BEV Alignment"
title_zh: "CalibBEV: 基于BEV对齐的激光雷达-相机标定"
authors: "Filippo D'Addeo, Lorenzo Cipelli, Adriano Cardace, Emanuele Ghelfi, Andrea Zinelli, Massimo Bertozzi"
date: 2026-08-03
pdf: "https://arxiv.org/pdf/2608.02309v1"
tags: ["query:multimodal"]
score: 9.0
evidence: 基于BEV的特征对齐与对比学习损失用于激光雷达-相机标定
tldr: "针对激光雷达与相机标定中跨模态特征差异大、难以直接匹配的问题，提出CalibBEV方法，将两种传感器数据统一到鸟瞰图（BEV）空间表示。通过域特定网络提取BEV特征，利用对比损失实现语义一致性约束，先回归粗略标定矩阵，再在BEV空间显式对齐特征进行精修。在KITTI和nuScenes上，相对旋转误差分别降低51%和68%，相对平移误差降低80%和91%，显著优于已有方法，实现高精度鲁棒标定。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.02309v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 731, \"height\": 457, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02309v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1710, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02309v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 867, \"height\": 152, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.02309v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1817, \"height\": 504, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.02309v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1450, \"height\": 544, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02309v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 820, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02309v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 571, \"height\": 167, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02309v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 788, \"height\": 169, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02309v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 643, \"height\": 253, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.02309v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 737, \"height\": 260, \"label\": \"Table\"}]"
motivation: 现有基于点像素匹配的标定方法精度低，缺乏统一的跨模态表示，难以直接对齐异源数据。
method: 将LiDAR与相机数据统一为BEV特征，先用对比学习对齐语义空间，再通过两阶段粗到精的变换矩阵回归与显式特征对齐实现标定。
result: "在KITTI和nuScenes上，相对旋转误差分别下降51%和68%，相对平移误差下降80%和91%，达到最先进精度。"
conclusion: BEV统一表示与两阶段对齐有效提升标定精度，为跨模态标定提供了新框架。
---

## 摘要
我们提出了CalibBEV，一种新颖的基于鸟瞰图（BEV）对齐的激光雷达-相机标定方法。我们的方法将激光雷达和相机数据统一到共享的3D空间表示中，实现了准确且鲁棒的跨模态标定。CalibBEV使用特定领域的架构从每种模态中提取传感器特定的BEV特征，并通过两步对齐过程估计标定矩阵。首先，我们通过直接从BEV特征回归粗略的标定矩阵来执行隐式对齐。为了简化这种对齐，我们使用受CLIP启发的对比损失强制不同模态的BEV表示之间的语义一致性，引导两个网络朝向统一的特征空间。在第二步中，我们利用我们的BEV公式显式地将一种模态的特征与另一种模态对齐，将初始粗略估计细化为最终更准确的标定矩阵。CalibBEV显著优于先前的点对像素匹配方法，实现了最先进的标定精度。在KITTI和nuScenes基准测试中，与先前方法相比，我们的方法分别将相对旋转误差（RRE）降低了51%和68%，以及将相对平移误差（RTE）降低了80%和91%.

## Abstract
We present CalibBEV, a novel Bird's Eye View (BEV) alignment approach for LiDAR-camera calibration. Our method unifies LiDAR and camera data into a shared 3D spatial representation, enabling accurate and robust cross-modal calibration. CalibBEV extracts sensor-wise BEV features from each modality using domain-specific architectures and estimates the calibration matrix through a two-step alignment process. First, we perform an implicit alignment by regressing a coarse calibration matrix directly from the BEV features. To ease this alignment, we enforce semantic consistency between BEV representations across modalities using a contrastive loss inspired by CLIP, guiding both networks toward a unified feature space. In the second step, we leverage our BEV formulation to explicitly align the features of one modality with the other, refining the initial coarse estimate into a final, more accurate calibration matrix. CalibBEV significantly outperforms prior point-to-pixel matching methods, achieving state-of-the-art calibration accuracy. On the KITTI and nuScenes benchmarks, our method reduces the Relative Rotation Error (RRE) by 51% and 68%, and the Relative Translation Error (RTE) by 80% and 91%, respectively, compared to previous methods.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：激光雷达与相机之间的外参标定是自动驾驶、3D视觉和机器人领域的核心问题。传统方法多依赖点对像素（point-to-pixel）匹配，但跨模态特征天然存在语义鸿沟，难以直接匹配，导致标定精度和鲁棒性不足。
- **核心问题**：如何将来自不同传感器、不同特征空间的数据统一起来，并准确估计两者之间的刚体变换（旋转和平移）。
- **整体含义**：论文提出将图像到点云配准问题重新表述为鸟瞰图（Bird’s Eye View, BEV）对齐问题，通过BEV这一共享的3D空间表示，使跨模态对齐变得更为自然和可解释，从而实现高精度、鲁棒的标定。

## 2. 论文提出的方法论

- **核心思想**：将RGB图像和LiDAR点云分别通过域特定网络编码为2D/3D特征，再提升（lift）并投影到统一的3D BEV空间中，形成两个BEV特征图。随后通过“隐式对齐 + 显式对齐”两步流程，从粗到精地估计标定矩阵。
- **网络架构**：
  - 图像分支：ResNet-50提取多尺度特征，经过上采样、拼接和CNN编码得到2D特征 `F_rgb`。
  - 点云分支：Point Transformer提取逐点特征 `F_pts`。
  - 两者经过线性/卷积层统一特征维度后，通过坐标网格投影和特征采样/散落（scatter）生成3D BEV特征 `3D F_rgb` 和 `3D F_pts`。
- **隐式对齐（Implicit Alignment）**：
  - 将两个BEV特征在通道维度拼接，送入CNN解码器（ResNet-18）和旋转/平移预测头，直接回归粗标定矩阵 `T_coarse`。
  - 解码器依靠大感受野对全局几何关系进行推理，隐式学习对齐参数。
- **CLIP对比损失**：
  - 利用已知的真实标定矩阵，建立像素与点之间的对应关系，构造相似度矩阵。
  - 使用类似CLIP的对称交叉熵损失 `L_clip`，促使两个分支对对应点/像素学习相似特征，缩小模态间特征差异。
- **显式对齐（Explicit Alignment）**：
  - 冻结隐式对齐阶段的所有模块，利用 `T_coarse` 对LiDAR BEV特征进行可微分的反向扭曲（backward warping），使其与RGB BEV特征预对齐。
  - 将预对齐后的特征再次融合并送入新解码器，输出细标定矩阵 `T_fine`，最终变换为 `T = T_fine · T_coarse`。
- **公式要点**：
  - 3D坐标投影：`(u, v, d)^T = K · g`
  - 相似度矩阵：`S = e^{γ (F_rgb · F_pts^T)}`
  - CLIP损失：`L_clip = (CE(S, Ŝ) + CE(S^T, Ŝ^T)) / 2`
  - 总体损失：`L = α L_clip + L_r + L_t`（`L_r` 为旋转L1损失，`L_t` 为平移L2损失）

## 3. 实验设计

- **数据集**：
  - **KITTI odometry**：使用序列00-08训练，09-10测试；采用左右双目相机图像。
  - **nuScenes**：使用官方700个训练场景、150个验证场景、150个测试场景；默认使用前视相机和LiDAR点云。
- **基准设置**：
  - 标准benchmark为沿垂直轴随机旋转 ±360°，并随机平移 ±10m（前后、左右方向）。
  - 也额外测试了6DoF场景（各轴旋转 ±20°，平移 ±1.5m）。
- **对比方法**：
  - 点云方法：DeepI2P、CorrI2P、VP2P-Match、CurrI2P、RelaI2P、ICLM、GraphI2P等。
  - 投影方法：CalibNet、LCCNet。
  - 指标：平均相对平移误差（RTE）、平均相对旋转误差（RRE）、注册准确率（Acc，RTE<2m且RRE<5°的比例），并报告标准差。

## 4. 资源与算力

- 论文正文未明确说明使用的GPU型号、数量、具体训练硬件资源或训练总时长（仅提到训练迭代次数为260k，显式对齐阶段额外120k迭代）。因此，**无法从论文中获取详细的算力配置信息**，只能确认训练轮次和优化器设置（Adam，初始学习率1e-4/1e-5，批量大小8）。

## 5. 实验数量与充分性

- **实验数量**：
  - 两个标准数据集（KITTI和nuScenes）上的SOTA对比。
  - 消融实验：基线、+隐式对齐、+CLIP损失、+显式对齐共4组，在两个数据集上均有结果。
  - 泛化实验：KITTI上仅用左相机训练、右相机测试的跨相机泛化。
  - 多相机分析：nuScenes上单相机 vs 6相机对比。
  - 6DoF小幅度扰动分析（与投影/点云方法对比）。
  - BEV分辨率/网格范围分析（5组配置）。
  - 定性可视化（特征相似性热力图、对齐结果对比）。
- **充分性与公平性**：
  - 消融覆盖全面，验证了每个组件的贡献。
  - 对比实验包含多种类型的方法，并遵循已有文献的基准设置和指标，较为公平。
  - 跨相机泛化实验增强了结论的可靠性。
  - 但论文指出未像部分工作那样去除大误差样本（他们刻意保留以反映真实性能），这与其他方法可能不完全一致，但更客观。

## 6. 论文的主要结论与发现

- **核心结论**：将图像到点云配准建模为BEV对齐任务可行且有效。
- **性能提升**：
  - 在KITTI上，RTE为0.04m，RRE为0.61°，相比先前最优方法（ICLM）RTE降低0.16m、RRE降低0.63°，准确率提升2.47%。
  - 在nuScenes上，RTE为0.04m，RRE为0.54°，相比GraphI2P显著更优。
  - 相比已有方法，RRE在KITTI/nuScenes上分别降低51%/68%，RTE降低80%/91%。
- **鲁棒性**：标准差显著低于所有对比方法，说明结果稳定。
- **可扩展性**：框架可自然扩展到多相机设置，6相机配置下性能进一步提升（RRE从0.54°降至0.28°，准确率100%）。
- **消融验证**：隐式对齐提供核心能力，CLIP损失改善跨模态特征一致性，显式对齐进一步精修。

## 7. 优点

- **新颖视角**：首次将LiDAR-camera标定问题转化为BEV对齐问题，利用BEV的几何统一性规避了直接点-像素匹配的困难。
- **两阶段粗到精设计**：隐式对齐估计粗参数，显式对齐利用BEV空间的可扭曲性进行精修，逻辑清晰且有效。
- **跨模态特征对齐**：引入CLIP风格的对比损失，强化语义一致性，技术选型合理。
- **架构灵活**：不限制特定模态网络，可使用任意2D/3D编码器；易于扩展到多相机系统。
- **实验全面**：包含标准数据集、消融、泛化、多相机、6DoF、分辨率分析等，覆盖维度广。
- **性能卓越**：在多个指标和数据集上达到SOTA，且鲁棒性优势明显。

## 8. 不足与局限

- **两阶段训练非端到端**：隐式对齐和显式对齐分开训练，训练流程复杂，可能无法达到全局最优。论文也承认未来工作将考虑端到端联合优化。
- **算力/硬件信息缺失**：未报告GPU型号、数量、训练时间等，不利于复现时评估资源需求。
- **依赖BEV网格分辨率**：性能与BEV体素大小直接相关（实验中0.25m Cell最优），高分辨率会牺牲推理时间，存在精度-速度权衡。
- **点云大小受限**：需要对点云进行固定规模采样（N=40960），可能丢失远距离或稀疏区域的细节。
- **默认评估场景较受约束**：标准benchmark只考虑绕垂直轴的旋转和水平面平移，虽额外做了6DoF实验，但未完全覆盖真实行驶中的剧烈颠簸或俯仰变化。
- **跨传感器泛化未深入验证**：仅做了KITTI左右相机泛化，未测试跨数据集（如KITTI训练→nuScenes测试）的零样本表现。
- **定性分析有限**：只展示少量成功案例，失败案例仅提及在补充材料中，正文分析不够充分。

（完）
